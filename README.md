# 🤖 AI Chat с Swift Playground

Десктопное приложение для macOS, которое объединяет AI чат с возможностью выполнения и тестирования Swift кода.

## ✨ Основные возможности

### 🗣️ AI Чат
- Интеграция с OpenAI API через прокси
- Поддержка русского языка
- Интеграция с GitHub через MCP (Model Context Protocol)
- Сохранение API ключей и токенов

### 🚀 Swift Code Execution
- **Docker контейнеры** - безопасное выполнение в изолированной среде
- **Native Swift** - быстрое выполнение как fallback
- Автоматическое определение метода выполнения
- Подробные результаты выполнения

### 🧪 **НОВОЕ: Автоматическое тестирование Swift кода**
- **Генерация unit тестов** - AI создает comprehensive тесты для вашего кода
- **Выполнение тестов** - тесты запускаются в Docker контейнерах
- **Автоматическое исправление** - AI исправляет код на основе результатов тестов
- **Итеративное тестирование** - цикл повторяется до успешного прохождения всех тестов
- **Подробная отчетность** - детальная информация о каждой итерации

## 🏗️ Архитектура

### Основные компоненты
- **SwiftUI** - современный UI для macOS
- **Docker** - изолированное выполнение кода
- **MCP (Model Context Protocol)** - интеграция с внешними сервисами
- **AI Integration** - OpenAI API для генерации тестов и исправления кода

### Компоненты тестирования
1. **SwiftTestGenerator** - генерирует unit тесты с помощью AI
2. **SwiftTestRunner** - выполняет тесты в Docker контейнерах
3. **SwiftCodeFixer** - автоматически исправляет код на основе результатов тестов
4. **SwiftTestOrchestrator** - управляет полным циклом тестирования

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
# Установка Python зависимостей для MCP Docker сервиса
cd mcp-docker
python -m venv venv
source venv/bin/activate  # На macOS/Linux
pip install -r requirements.txt
```

### 2. Запуск MCP Docker сервиса

```bash
cd mcp-docker
source venv/bin/activate
python app.py
```

### 3. Запуск приложения

Откройте `AIChat.xcodeproj` в Xcode и запустите схему `AIChatMac`.

## 📖 Использование

### Обычное выполнение Swift кода

Отправьте Swift код в чат:

```
print("Hello, World!")
let x = 2 + 2
print("2 + 2 = \(x)")
```

### **Автоматическое тестирование Swift кода**

Отправьте код с ключевыми словами для тестирования:

```
Проверь этот код:
class Calculator {
    func add(_ a: Int, _ b: Int) -> Int {
        return a + b
    }
}
```

**Ключевые слова для активации тестирования:**
- `тест`
- `test` 
- `проверь`
- `проверить`
- `unit`
- `юнит`

### Примеры использования тестирования

#### Пример 1: Простой класс
```
Создай тесты для этого класса:
class StringProcessor {
    func reverse(_ text: String) -> String {
        return String(text.reversed())
    }
}
```

#### Пример 2: Класс с ошибками (будет автоматически исправлен)
```
Проверь этот код:
class BuggyCalculator {
    func add(_ a: Int, _ b: Int) -> Int {
        return a - b  // Ошибка
    }
}
```

## 🔧 Конфигурация

### API Ключи
- **OpenAI API Key** - для AI функций
- **GitHub Token** - для интеграции с GitHub

### Entitlements
Приложение настроено для работы с Docker:
- `com.apple.security.app-sandbox = false` - отключен sandbox для доступа к Docker
- Сетевые разрешения для API запросов
- Разрешения для Docker socket

## 📊 Результаты тестирования

После выполнения тестирования вы получите подробный отчет:

### ✅ Успешное тестирование
- Финальный код (исправленный, если были ошибки)
- Сгенерированные unit тесты
- Статистика по итерациям
- Время выполнения

### ❌ Неудачное тестирование
- Причина неудачи
- Количество попыток исправления
- Детали по каждой итерации

## 🛠️ Технические детали

### Среда выполнения
- **Docker контейнеры** (приоритет) - `swift:5.9`
- **Native Swift** (fallback) - системный Swift компилятор
- **Изолированная среда** для безопасности

### Генерация тестов
- Использует AI для создания comprehensive тестов
- Покрывает все публичные методы
- Включает edge cases и граничные условия

### Автоматическое исправление
- Анализирует ошибки тестов
- Генерирует исправленный код
- Сохраняет логику и структуру
- Максимум 5 итераций исправления

## 🔍 Диагностика

Приложение включает встроенные инструменты диагностики:

### Docker диагностика
- Проверка доступности Docker
- Проверка версии Docker
- Тестирование подключения к Docker daemon

### Общая диагностика
- Проверка MCP Docker сервиса
- Проверка Native Swift исполнителя
- Тестовое выполнение кода

## 📁 Структура проекта

```
AIChat/
├── AIChat/                 # iOS версия
├── AIChatMac/             # macOS версия
│   ├── SwiftTestGenerator.swift      # Генерация тестов
│   ├── SwiftTestRunner.swift         # Выполнение тестов
│   ├── SwiftCodeFixer.swift          # Исправление кода
│   ├── SwiftTestOrchestrator.swift   # Управление тестированием
│   └── ...
├── mcp-docker/            # MCP Docker сервис
│   ├── app.py             # Flask сервер
│   ├── requirements.txt   # Python зависимости
│   └── ...
└── README.md
```

## 🚨 Ограничения

### Тестирование
- Максимум 5 итераций автоматического исправления
- Поддерживаются только базовые Swift конструкции
- Требуется подключение к интернету для AI функций

### Выполнение кода
- Ограниченная поддержка сложных Swift фич
- Таймаут выполнения: 30 секунд
- Изолированная среда без доступа к файловой системе

## 🤝 Вклад в проект

1. Fork репозитория
2. Создайте feature branch
3. Внесите изменения
4. Добавьте тесты
5. Создайте Pull Request

## 📄 Лицензия

MIT License

## 🆘 Поддержка

При возникновении проблем:
1. Проверьте диагностику в приложении
2. Убедитесь, что Docker запущен
3. Проверьте подключение к интернету
4. Проверьте правильность API ключей